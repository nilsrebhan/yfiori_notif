_schema-version: "3.2"
ID: compagflpnotifications
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: compagflpnotifications-destination-content
  type: com.sap.application.content
  requires:
  - name: compagflpnotifications-destination-service
    parameters:
      content-target: true
  - name: compagflpnotifications-repo-host
    parameters:
      service-key:
        name: compagflpnotifications-repo-host-key
  - name: compagflpnotifications-uaa
    parameters:
      service-key:
        name: compagflpnotifications-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: compagflpnotifications_html_repo_host
          ServiceInstanceName: compagflpnotifications-html5-service
          ServiceKeyName: compagflpnotifications-repo-host-key
          sap.cloud.service: compagflpnotifications
        - Authentication: OAuth2UserTokenExchange
          Name: compagflpnotifications_uaa
          ServiceInstanceName: compagflpnotifications-xsuaa-service
          ServiceKeyName: compagflpnotifications-uaa-key
          sap.cloud.service: compagflpnotifications
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: compagflpnotifications-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: compagflpnotifications-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - compagflpnotifications.zip
      name: compagflpnotifications
      target-path: resources/
- name: compagflpnotifications
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: compagflpnotifications-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: compagflpnotifications-destination-service
    service-plan: lite
- name: compagflpnotifications-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: compagflpnotifications-xsuaa-service
    service-plan: application
- name: compagflpnotifications-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: compagflpnotifications-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
